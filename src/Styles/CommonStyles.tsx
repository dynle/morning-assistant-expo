import { StyleSheet } from "react-native";

export const commonStyle = StyleSheet.create({
    buttonStyle: {
        minWidth: "65%",
        borderRadius: 20,
        backgroundColor: "#F3EDE1",
    },
});
